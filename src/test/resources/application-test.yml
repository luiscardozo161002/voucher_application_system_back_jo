spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  cache:
    type: none

app:
  rate-limit:
    enabled: false

logging:
  level:
    mx.juarezdeoriente: DEBUG
    org.flywaydb: INFO